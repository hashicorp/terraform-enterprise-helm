# Test values for service port exposure with accessMode=port
# Expected: Service should expose admin-https-port (8443)

image:
  repository: images.releases.hashicorp.com
  name: hashicorp/terraform-enterprise
  tag: v202501-1

tfe:
  adminConsole:
    accessMode: "port"

service:
  type: ClusterIP

env:
  variables:
    TFE_HOSTNAME: "test.example.com"
  secrets:
    TFE_ENCRYPTION_PASSWORD: "test-password"
    TFE_LICENSE: "test-license"
